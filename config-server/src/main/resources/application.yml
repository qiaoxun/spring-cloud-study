server:
  port: 8001
spring:
  application:
    name: spring-cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/qiaoxun/spring-cloud-config-rep/     # 配置git仓库的地址
